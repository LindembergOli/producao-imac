name: Deploy para Produção

on:
  push:
    branches: [main]
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy para Produção
    runs-on: ubuntu-latest
    # environment: production  # Descomente após criar o ambiente 'production' nas Configurações do GitHub
    
    steps:
      - name: Fazer checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_KEY }}
      
      - name: Adicionar servidor aos hosts conhecidos
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.PROD_SSH_HOST }} >> ~/.ssh/known_hosts
      
      - name: Criar arquivo .env
        run: |
          echo "${{ secrets.PROD_ENV_FILE }}" > .env
      
      - name: Copiar arquivos para o servidor
        run: |
          scp docker-compose.prod.yml ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }}:~/imac-app/
          scp nginx-proxy.conf ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }}:~/imac-app/
          scp .env ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }}:~/imac-app/
      
      - name: Fazer backup do deployment atual
        run: |
          ssh ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} << 'EOF'
            cd ~/imac-app
            
            # Criar backup
            BACKUP_DIR=~/backups/$(date +%Y%m%d_%H%M%S)
            mkdir -p $BACKUP_DIR
            
            # Backup do banco de dados
            docker-compose -f docker-compose.prod.yml exec -T postgres \
              pg_dump -U $POSTGRES_USER $POSTGRES_DB > $BACKUP_DIR/database.sql
            
            echo "✅ Backup created at $BACKUP_DIR"
          EOF
      
      - name: Fazer deploy via SSH
        run: |
          ssh ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} << 'EOF'
            cd ~/imac-app
            
            # Baixar imagens mais recentes
            docker-compose -f docker-compose.prod.yml pull
            
            # Parar containers atuais
            docker-compose -f docker-compose.prod.yml down
            
            # Iniciar novos containers
            docker-compose -f docker-compose.prod.yml up -d
            
            # Executar migrações
            sleep 15
            docker-compose -f docker-compose.prod.yml exec -T backend npx prisma migrate deploy
            
            # Mostrar status
            docker-compose -f docker-compose.prod.yml ps
          EOF
      
      - name: Verificação de Saúde
        run: |
          sleep 20
          curl -f http://${{ secrets.PROD_SSH_HOST }}/health || exit 1
      
      - name: Reverter em Caso de Falha
        if: failure()
        run: |
          ssh ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} << 'EOF'
            cd ~/imac-app
            
            echo "❌ Deployment failed! Rolling back..."
            
            # Obter backup mais recente
            LATEST_BACKUP=$(ls -t ~/backups/ | head -1)
            
            # Restaurar banco de dados
            docker-compose -f docker-compose.prod.yml exec -T postgres \
              psql -U $POSTGRES_USER $POSTGRES_DB < ~/backups/$LATEST_BACKUP/database.sql
            
            # Reiniciar com versão anterior
            docker-compose -f docker-compose.prod.yml up -d
            
            echo "✅ Rollback completed"
          EOF
      
      - name: Resumo do Deployment
        if: success()
        run: |
          echo "✅ Deployment to Production successful!"
          echo "🔗 Application: http://${{ secrets.PROD_SSH_HOST }}"
          echo "📊 Version: ${{ github.sha }}"
