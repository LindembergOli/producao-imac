# ================================
# Variáveis de Ambiente - Desenvolvimento Local
# IMAC Congelados - Sistema de Controle de Produção
# ================================

# ================================
# PostgreSQL Database
# ================================
# Usuário do banco de dados
POSTGRES_USER=imac_user

# Senha do banco de dados (altere em produção!)
POSTGRES_PASSWORD=imac_password

# Nome do banco de dados
POSTGRES_DB=imac_congelados

# ================================
# Backend API
# ================================
# Ambiente de execução (development, production, test)
NODE_ENV=development

# Porta do servidor backend
PORT=3001

# Host do servidor (0.0.0.0 para aceitar conexões externas)
HOST=0.0.0.0

# URL de conexão com PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Em Docker, use o nome do serviço (postgres) como host
# Em desenvolvimento local, use localhost
DATABASE_URL=postgresql://imac_user:imac_password@postgres:5432/imac_congelados?schema=public

# ================================
# JWT (JSON Web Tokens)
# ================================
# Secret para assinar tokens JWT
# IMPORTANTE: Gere uma string aleatória forte em produção!
# Comando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=dev_jwt_secret_change_in_production_min_32_chars

# Tempo de expiração do token de acesso
# Exemplos: 15m, 1h, 7d, 30d
JWT_EXPIRES_IN=7d

# Secret para tokens de refresh
# IMPORTANTE: Deve ser diferente do JWT_SECRET!
JWT_REFRESH_SECRET=dev_refresh_secret_change_in_production_min_32

# Tempo de expiração do refresh token
JWT_REFRESH_EXPIRES_IN=30d

# ================================
# Segurança
# ================================
# Origens permitidas para CORS
# Em desenvolvimento: http://localhost:3000
# Em produção: https://seudominio.com
# Múltiplas origens: http://localhost:3000,https://seudominio.com
CORS_ORIGIN=http://localhost:3000

# Rate Limiting - Janela de tempo em milissegundos
# 900000ms = 15 minutos
RATE_LIMIT_WINDOW_MS=900000

# Rate Limiting - Máximo de requisições por janela
RATE_LIMIT_MAX_REQUESTS=100

# ================================
# Logs
# ================================
# Nível de log (error, warn, info, http, verbose, debug, silly)
# Desenvolvimento: debug
# Produção: info ou warn
LOG_LEVEL=debug

# ================================
# Frontend (Vite)
# ================================
# URL da API para o frontend
# Em desenvolvimento local: http://localhost:3001/api
# Em Docker (dev): http://localhost:3001/api
# Em produção com Nginx: /api (relativo)
VITE_API_URL=http://localhost:3001/api

# ================================
# Configurações Opcionais
# ================================
# Porta do Vite dev server (padrão: 3000)
# VITE_PORT=3000

# Habilitar HTTPS no Vite (true/false)
# VITE_HTTPS=false

# ================================
# Notas Importantes
# ================================
# 1. Este arquivo é para DESENVOLVIMENTO LOCAL
# 2. NUNCA commite este arquivo com senhas reais
# 3. Para produção, use .env.production com valores seguros
# 4. Gere secrets fortes usando:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 5. Em Docker, ajuste DATABASE_URL para usar 'postgres' como host
# 6. Em local, ajuste DATABASE_URL para usar 'localhost' como host
